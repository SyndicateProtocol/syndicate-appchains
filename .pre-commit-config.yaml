exclude: vendor|\.pb\.go

repos:
  - repo: https://github.com/Bahjat/pre-commit-golang
    rev: v1.0.3
    hooks:
      - id: go-fmt-import
        files: ^(metabased-publisher)/
      - id: go-vet
        files: ^(metabased-publisher)/
      - id: go-lint
        files: ^(metabased-publisher)/
      - id: golangci-lint # requires github.com/golangci/golangci-lint
        files: ^(metabased-publisher)/
        args: [--config=.golangci.yaml --allow-parallel-runners]
